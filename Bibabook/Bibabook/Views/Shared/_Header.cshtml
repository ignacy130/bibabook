@{
    AjaxOptions options = new AjaxOptions
    {
        UpdateTargetId = "main-content",
        InsertionMode = InsertionMode.Replace,
    };
}
<header>

    <!-- A menu bar. -->
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <!--float-left-->
            <div class="navbar-header ">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <p class="site-title">@Html.ActionLink("Bibabook", "Index", "Home")</p>
            </div>

            <nav>
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                        @using (Ajax.BeginForm("Search", "AppUsers", options))
                        {
                            <div class="form-group form-inline col-sm-5">
                                @Html.TextBox("name", "", new { @class = "form-control" })
                                <button class="btn btn-default pull-right col-sm-3" type="submit">Szukaj</button>
                            </div>
                        }

                    <ul class="nav navbar-nav" id="operations">
                        
                        <li>@Html.ActionLink("Dodaj wydarzenie", "Create", "AppEvents")</li>
                        <li>@Html.ActionLink("Wszystkie wydarzenia", "List", "AppEvents")</li>
                        <li>@Html.ActionLink("Znajomi", "MyFriends", "AppUsers")</li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" style="color:red;" data-toggle="dropdown" role="button" aria-expanded="false">
                                @ViewBag.EventCount <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu" role="menu">
                                @*@foreach (var link in ViewBag.EventLinks)
                                {
                                    <li>@link</li>
                                }*@
                            </ul>
                        </li>
                    </ul>
                    <div class="navbar-right">
                        
                    </div>
                </div>
            </nav>



        </div>
    </nav>
</header>